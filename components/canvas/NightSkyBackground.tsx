'use client';

import React from 'react';
import GlobalSpaceBackground from './GlobalSpaceBackground';

export const NightSkyBackground: React.FC = () => {
  return <GlobalSpaceBackground />;
};

export default NightSkyBackground;
